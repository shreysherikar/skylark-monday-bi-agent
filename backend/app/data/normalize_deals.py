"""Deals board normalization layer.

Implements repeated-header filtering, client code normalization, stage mapping,
closure probability validation, and numeric deal value parsing for the Deals board.

Specification defined in NORMALIZATION_NOTES.md.
Logic to be implemented in Phase 1 (Core Build).
"""
