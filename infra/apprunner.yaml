version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install --upgrade pip
      - cd backend && pip install .
run:
  command: uvicorn app.main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
    env: APP_PORT
